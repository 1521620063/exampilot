{
  "name": "exampilot-extension",
  "private": true,
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:package": "NO_MINIFY=true node scripts/build.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "preact": "^10.26.0",
    "htm": "^3.1.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
