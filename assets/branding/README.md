# ExamPilot Branding

`icon.png` is the shared high-resolution source image for ExamPilot branding.

Platform-specific icons remain in `chrome-extension/icons/` and `desktop-app/src-tauri/icons/` because Chrome and Tauri require different sizes, formats, and directory layouts. Regenerate those platform assets from this source when the brand icon changes.
